# تحديث مصدر المقرر — 2026-09-09

المصدر: [AljawharaAlbahlalDev/SDA-AIE-211-Bayan-Course](https://github.com/AljawharaAlbahlalDev/SDA-AIE-211-Bayan-Course/tree/7949de02de71cd3ae644cd89f766dfc73aa9b7f4)، عند commit `7949de02de71cd3ae644cd89f766dfc73aa9b7f4`.

قارنّا كل ملف متتبّع في المصدر مع نسخة البداية `b63109f` ومع تنفيذ BAYAN.DAICO الحالي. التغييرات الجديدة في المصدر محصورة في `README.md` وملف `LAB1-Sol`. نُقلا كاملين كما هما، ونسبة الحل تبقى لصاحب المستودع الأصلي. المكتبات والبيانات والاختبارات و`docs/LABS.md` في المصدر تطابق نسخة البداية؛ لم تصل بيانات مصححة أو حلول تنفيذية جديدة للاب ٢ أو ٣.

## الحل المنقول والتنفيذ الحالي

[`LAB1-Sol`](../LAB1-Sol) يجمع حل preprocessing وsentence segmentation وتدقيق tokenizers في ملف تعليمي واحد. احتفظنا به كمرجع أصلي؛ وظائفه الأساسية مطبقة بالفعل في `src/bayan/preprocessing/` و`notebooks/01_tokenizer_audit.py`، مع دعم إضافي لأرقام الهاتف المنسقة وإصلاح حدود الجمل وتقرير UNK. لم نستبدل تنفيذنا بقوالب `NotImplementedError` التي ما زالت موجودة في ملفات المصدر.

بقيت إضافتا `accelerate` و`sentencepiece` في متطلباتنا لتشغيل التدريب. النتائج في `BENCHMARKS.md` تخص تشغيلنا الفعلي؛ الأرقام داخل حل المصدر أمثلة من المصدر.

## مطابقة المتطلبات الجديدة

- README الجديد، Lab 3B Step 4، يطلب **12/12 answerable spans و0 حالات unanswerable**. أما `docs/LABS.md` في المصدر فما زال يطلب 9+3. التقييم المنفصل يوضح التغيير الصريح في README. بناءً على طلب المستخدم إبقاء ملفات متطلبات المقرر كما هي، أُعيدت `docs/LABS.md` إلى نص المصدر الأصلي؛ التعارض موثق هنا ولا يُحسم بتعديل قائمة المتطلبات.
- التنبؤات المحفوظة سابقًا حققت 12/12. سجّلنا تقييمها مقابل المتطلب الجديد في [`qa_course_update.json`](../artifacts/lab3/qa_course_update.json)، مع بصمة التقرير والبيانات. بقيت تقارير التشغيل الأصلية دون تغيير، ولم نعد الاستدلال على الاختبار. يكتب `scripts/qa_smoke.py` تقييم المتطلب الجديد في التشغيلات القادمة.
- وجود صفر حالات بلا إجابة لا يثبت جودة رفض الإجابة؛ تجربة 9+3 الإضافية تبقى دليلًا منفصلًا.
- هدف التصنيف **+8 نقاط macro-F1** لم يتغيّر ولم يتحقق؛ الدليل الجديد لم يغيّر بيانات الاختبار ذات الفئات الأربع أو نتيجتنا ذات الفرق صفر.
- أصبحت مقارنة النماذج العربية في Lab 4 Step 4 اختيارية للتدريب الإضافي. قائمة اللاب الأصلية محفوظة كما وردت من المصدر، ووصف الاختيارية من README موثق هنا فقط. إرشادات CAMeL وColab للابات ٤ و٥ نُقلت ضمن README؛ تنفيذ تلك اللابات ليس ضمن عملية النقل الحالية.
- يطلب الدليل استخدام قرار tokenizer من لاب ١، ويعرض CAMeLBERT و`max_length=256` في الرسم. قرارنا المقاس هو XLM-R للنصوص العربية والإنجليزية، وطول 64 موثّق في تقارير التشغيل. أبقينا القرار والتجربة المسجلين؛ لم نُعد تدريب نموذج مختلف بناءً على نتائج الاختبار النهائي.

## التشغيل

دفترنا المجهز: [`03_lab3_colab.ipynb`](../notebooks/03_lab3_colab.ipynb). يسحب كود هذا المستودع ويحفظ النماذج في Drive. نقل الملفات إلى GitHub لا يحدّث تلقائيًا نسخة الدفتر المفتوحة في Colab.

التحقق: **67 اختبارًا ناجحًا** للاب ١–٣ وحالات تقييم QA الجديدة، وخلايا الدفتر سليمة نحويًا، و`pip check` لا يرصد تعارضات. لا يشمل إعادة تدريب النماذج أو تشغيل لابات ٤–٧ التي ما زالت قوالب في المشروع.

## الحفاظ على نص متطلبات المقرر

أُعيدت `docs/LABS.md` إلى النسخة الأصلية المطابقة لمصدر المقرر. تبقى النتائج والتفسيرات في `BENCHMARKS.md` وملفات الشرح، ولا تغيّر معايير النجاح الأصلية. `requirements.txt` سبق أن أضيفت إليه `accelerate` و`sentencepiece` لتشغيل لاب ٣؛ لم يُعدّل في هذه الخطوة. بيانات المصدر لم تُعدّل أو تُترجم.
